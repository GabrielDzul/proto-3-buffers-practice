syntax = "proto3";

/*
    Defining multiple messages in a same file is possible
*/
message MessageOne {
    
}

enum EnumOne {
    ENUM_ONE_UNSPECIFIED = 0;
    //...
}

message MessageTwo {
    MessageOne one = 1;
    EnumOne tow = 2;
    int32 test = 3;
}